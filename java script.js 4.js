// prompt - sync
// Node Packager manager - npm install prompt - sync 
const prompt = require('prompt-sync')();

const userName = prompt('Type your name: ');

console.log(`the typed name was: ${userName} `);

